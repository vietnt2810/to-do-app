import { createStitches } from "@stitches/react";

export const { styled } = createStitches({
  theme: {
    colors: {
      headerColor: "#343d46",
      greyColor: "#828385",
      buttonColor: "#343434",
      backgroundColor: "#c0c5ce",
      hoverColor: "#595959",
    },
  },
});
